
const initState = {
    navitemSelected: 'Home' // Home, Post, New Post, Editing Post
}

export default function (state = initState, action) {
    switch (action.type) {

        default:
            return state
    }
}